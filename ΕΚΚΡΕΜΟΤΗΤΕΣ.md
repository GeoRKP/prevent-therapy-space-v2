# Εκκρεμότητες — PREVENT Therapy Space

Τελευταία ενημέρωση: 19 Σεπτεμβρίου 2026

## 0. Checklist παράδοσης στον πελάτη

Τι είναι deployed: repo `GeoRKP/prevent-therapy-space-v2`, branch `main` → Vercel project
`prevent-therapy-space` → https://www.preventtherapy.gr (το preventtherapy.gr κάνει redirect στο www).

### Έγινε 19/9/2026 (τεχνικά)

- **`NEXT_PUBLIC_SITE_URL` στο Vercel** ήταν `http://localhost:3000` στο production → sitemap,
  JSON-LD και τα links ακύρωσης στα emails έδειχναν localhost. Τώρα:
  production `https://www.preventtherapy.gr`, preview `https://prevent-therapy-space.vercel.app`,
  development `http://localhost:3000`. Κεντρικό σημείο στον κώδικα: `lib/site.js` (`SITE_URL`).
- **SEO/social metadata server-side**: το HTML δεν είχε `<title>`, description, Open Graph,
  canonical (όλα μπαίνανε client-side από τον `HeadManager`). Τώρα `app/layout.jsx` είναι
  server component με metadata, κάθε route έχει `layout.jsx` (`lib/seo.js` → `pageMetadata`),
  JSON-LD αποδίδεται server-side (`lib/structured-data.js`), OG εικόνα `public/images/og-image.jpg`.
  Ο `HeadManager` συνεχίζει να ενημερώνει τα tags όταν ο επισκέπτης αλλάζει σε αγγλικά.
- **`CONTACT_EMAIL`** (παραλήπτης ειδοποιήσεων θεραπευτή + φόρμας επικοινωνίας) διαβάζεται
  πλέον από env (Vercel + `.env.local`) αντί για hardcoded τιμή. Στο Vercel μπήκε η τιμή που
  ίσχυε μέχρι τώρα (`geokap94@hotmail.com`) — βλ. §5 για την αλλαγή στο email του ιατρείου.
- `robots.txt` → sitemap στο www· `.env.example` μπαίνει πλέον στο repo (το `.env*` το αγνοούσε).
- Σελίδες «Υπηρεσίες» και «Σχετικά»: ο PageHero ήταν ίδιος με το heading της πρώτης ενότητας.
  Τώρα οι ενότητες `ServicesGrid` / `AboutFrenaSection` δέχονται `asPageIntro` και δεν
  ξαναγράφουν τον τίτλο. Το «Τα Εργαλεία μας» βγήκε από τη «Σχετικά» (υπάρχει στην αρχική)
  και το διπλό «Ιατρική Συνεργασία» έγινε «Στόχος η Αυτονομία σας».

### Χρειάζεται τον πελάτη πριν την παράδοση

| # | Θέμα | Ποιος | Τι ακριβώς |
|---|------|-------|------------|
| 1 | Resend (emails) | Πελάτης + dev | Βλ. §1. Χωρίς αυτό η φόρμα επικοινωνίας επιστρέφει σφάλμα και δεν στέλνεται κανένα email κράτησης. |
| 2 | Email ιατρείου | Πελάτης | Υπάρχει το `info@preventtherapy.gr`; Το site το δείχνει δημόσια (header, επικοινωνία, JSON-LD). Βλ. §5. |
| 3 | Google Calendar του φυσικοθεραπευτή | Πελάτης παρών + dev | Βλ. §6 — τώρα συνδεδεμένο το προσωπικό gmail του developer. |
| 4 | Νομικός έλεγχος | Πελάτης | Πολιτική απορρήτου / όροι χρήσης (§4). |
| 5 | Συνεργάτες / αθλητές | Πελάτης | Ονόματα + φωτογραφίες (§2, §3). |
| 6 | Κωδικός /admin | Dev | Νέο `ADMIN_PASSWORD` στο Vercel πριν την παράδοση· δίνεται στον πελάτη. |
| 7 | Ιδιοκτησία | Πελάτης + dev | Απόφαση: μεταφορά Vercel project / GitHub repo / domain στον πελάτη ή παραμονή στον dev με συμφωνία συντήρησης. |
| 8 | Google Business link | Πελάτης | Επιβεβαίωση rating/πλήθους κριτικών και ακριβούς link προφίλ (`data/reviews.js`). |
| 9 | Καθάρισμα Vercel env | Dev | Αχρησιμοποίητα από παλιό project: `AUTH_SECRET`, `DIRECT_DATABASE_URL`, `GOOGLE_SERVICE_ACCOUNT_EMAIL`, `GOOGLE_PRIVATE_KEY`, `ADMIN_EMAIL`, `sslmode`. Δεν βλάπτουν· μπορούν να σβηστούν. |

## 1. Resend — τα emails του συστήματος κρατήσεων (ΤΟ ΜΟΝΟ ΤΕΧΝΙΚΟ ΠΟΥ ΛΕΙΠΕΙ)

Ο κώδικας για όλα τα emails (επιβεβαίωση με link ακύρωσης, υπενθυμίσεις 24h/2h,
ακύρωση, μετάθεση, ειδοποιήσεις θεραπευτή, φόρμα επικοινωνίας, review request) είναι έτοιμος
και δοκιμασμένος. Δεν στέλνεται τίποτα γιατί το `RESEND_API_KEY` στο Vercel είναι placeholder
(`re_placeholder`). Συνέπειες σήμερα: η φόρμα επικοινωνίας επιστρέφει 500 (ο επισκέπτης βλέπει
σφάλμα)· οι κρατήσεις ολοκληρώνονται κανονικά (Google Calendar) και ο ασθενής παίρνει μόνο την
πρόσκληση ημερολογίου από τη Google, όχι το email επιβεβαίωσης με το link ακύρωσης.

Βήματα (μία φορά):

1. Λογαριασμός στο [resend.com](https://resend.com) (προτείνεται με τον λογαριασμό του ιατρείου).
2. **API Keys → Create API Key** → η τιμή μπαίνει:
   - στο `.env.local` (τοπικά) ως `RESEND_API_KEY`
   - στο Vercel → Environment Variables → `RESEND_API_KEY` (Edit → Save → Redeploy)
3. **Domains → Add domain → `preventtherapy.gr`** → προσθήκη των DNS records που
   δείχνει το Resend (SPF/DKIM) στον πάροχο του domain → αναμονή verify.
   - Μέχρι το verify, ο αποστολέας `onboarding@resend.dev` παραδίδει emails
     **μόνο στη διεύθυνση του κατόχου του Resend λογαριασμού** (αρκεί για δοκιμή).
4. Μετά το verify: αλλαγή του `EMAIL_FROM` (Vercel + `.env.local`) σε π.χ.
   `PREVENT Therapy Space <no-reply@preventtherapy.gr>`.

Παραλήπτης ειδοποιήσεων θεραπευτή: env `CONTACT_EMAIL` (Vercel). Σήμερα `geokap94@hotmail.com`.

## 2. Συνεργάτες χωρίς στοιχεία (αφαιρέθηκαν από το site 26/8/2026)

Οι κάρτες «Σύντομα κοντά μας» αφαιρέθηκαν. Για επαναφορά, μόλις υπάρξουν στοιχεία:

### Γυμνάστρια
- Χρειάζεται: όνομα, φωτογραφία, (προαιρετικά credentials/bio)
- Επαναφορά: προσθήκη entry `{ id: "trainer", image: "/images/team/….jpg", icon: Dumbbell }`
  στο `data/partners.js` + όνομα στα `public/locales/el|en/home.json`
  (`partners.members.trainer.name`). Τα υπόλοιπα λεκτικά (role, roleDesc) υπάρχουν ήδη.

### Γενική / Οικογενειακή Ιατρός
- Χρειάζεται: όνομα, φωτογραφία, (προαιρετικά credentials/bio)
- Επαναφορά: όπως παραπάνω με `id: "doctor"`, icon `Stethoscope`.

## 3. Αθλητές (χορηγίες) — section εκτός site (26/8/2026)

Το `AthletesSection` αφαιρέθηκε από την αρχική (`app/page.jsx`) γιατί τα ονόματα
ήταν placeholders («Όνομα Αθλητή»). Ο κώδικας υπάρχει ανέπαφος.

- Χρειάζεται: πραγματικά ονόματα + αθλήματα αθλητών που χορηγεί ο χώρος
- Επαναφορά: συμπλήρωση του `athletes.items` στα
  `public/locales/el|en/home.json` + επαναφορά των δύο γραμμών
  (`import { AthletesSection } …` και `<AthletesSection />`) στο `app/page.jsx`.

## 4. GDPR — ΕΓΙΝΕ (26/8/2026), εκκρεμεί νομικός έλεγχος

Υλοποιήθηκαν: σελίδες `/privacy` και `/terms` (δίγλωσσες, κείμενα στο
`public/locales/el|en/legal.json`), footer links, υποχρεωτικά checkboxes
συναίνεσης στη φόρμα κράτησης και επικοινωνίας (με server-side επιβολή στα
αντίστοιχα APIs). Cookie banner ΔΕΝ χρειάζεται — δεν υπάρχουν trackers/analytics·
αν προστεθούν ποτέ (π.χ. Google Analytics), τότε απαιτείται consent banner.

- Εκκρεμεί: έλεγχος των κειμένων πολιτικής απορρήτου και όρων χρήσης από
  νομικό — τα κείμενα είναι προσεκτικό πρότυπο, όχι νομική συμβουλή.

## 5. Email ιατρείου & λοιπά περιεχομένου

- **Email επικοινωνίας**: το site δείχνει δημόσια `info@preventtherapy.gr`
  (`header.json`, `contact.json`, `data/conditions.js`, JSON-LD), ενώ η Πολιτική Απορρήτου
  (`legal.json` el+en, 2 σημεία) και οι ειδοποιήσεις (`CONTACT_EMAIL`) πάνε στο
  `geokap94@hotmail.com`. Μόλις ο πελάτης επιβεβαιώσει ποιο mailbox υπάρχει:
  1. `legal.json` (el/en): αντικατάσταση του `geokap94@hotmail.com`.
  2. Vercel → `CONTACT_EMAIL` → το email του ιατρείου → Redeploy.
- ~~Τηλέφωνο site~~ — ΕΓΙΝΕ 26/8/2026: 697 295 2263 παντού (tel + Viber).
- **Φωτογραφική γλώσσα** (από design critique): η φωτό Τσιτουρίδη (κοστούμι,
  εξωτερική) δεν ταιριάζει με το ζεστό ύφος — ιδανικά νέα φωτογράφιση όλης της
  ομάδας μέσα στον χώρο.

## 6. Google Calendar — σύνδεση του λογαριασμού του φυσικοθεραπευτή

Σήμερα το `GOOGLE_REFRESH_TOKEN` (Vercel + `.env.local`) ανήκει στο προσωπικό gmail του
developer (giorgoskapatsinas@gmail.com): τα ραντεβού γράφονται στο δικό του ημερολόγιο και οι
προσκλήσεις στους ασθενείς φεύγουν από αυτόν. Για την παράδοση:

1. **Google Cloud console** (το project με το OAuth client): OAuth consent screen →
   αν το app είναι σε *Testing*, προσθήκη του Google λογαριασμού του ιατρείου στους
   test users **ή** Publish app. (Σε Testing τα refresh tokens λήγουν σε 7 ημέρες.)
2. Με τον φυσικοθεραπευτή παρόντα, τοπικά: `npm run google:setup` → ανοίγει η
   συγκατάθεση Google → συνδέεται **με τον λογαριασμό του ιατρείου** → το νέο
   `GOOGLE_REFRESH_TOKEN` γράφεται αυτόματα στο `.env.local`.
3. Vercel → Environment Variables → `GOOGLE_REFRESH_TOKEN` → νέα τιμή (production) → Redeploy.
4. `GOOGLE_CALENDAR_ID` μένει `primary` (ή id ξεχωριστού ημερολογίου «Ραντεβού», αν το
   φτιάξει ο πελάτης και το μοιραστεί — προτιμότερο για να μην μπλέκουν με προσωπικά events).
5. Δοκιμή: μία κράτηση από το site → το event εμφανίζεται στο ημερολόγιό του και ο
   ασθενής λαμβάνει την πρόσκληση από τον λογαριασμό του ιατρείου.

Σημείωση: τα δικαιώματα που ζητούνται είναι μόνο `calendar.events` + `calendar.freebusy`.
