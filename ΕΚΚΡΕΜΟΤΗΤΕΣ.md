# Εκκρεμότητες — PREVENT Therapy Space

Τελευταία ενημέρωση: 26 Αυγούστου 2026

## 1. Resend — τα emails του συστήματος κρατήσεων (ΤΟ ΜΟΝΟ ΤΕΧΝΙΚΟ ΠΟΥ ΛΕΙΠΕΙ)

Ο κώδικας για όλα τα emails (επιβεβαίωση με link ακύρωσης, υπενθυμίσεις 24h/2h,
ακύρωση, μετάθεση, ειδοποιήσεις θεραπευτή, φόρμα επικοινωνίας) είναι έτοιμος και
δοκιμασμένος. Δεν στέλνεται τίποτα γιατί το `RESEND_API_KEY` που υπήρχε ήταν
placeholder (`re_placeholder`) — δεν υπήρξε ποτέ πραγματικό κλειδί.

Βήματα (μία φορά):

1. Λογαριασμός στο [resend.com](https://resend.com) (προτείνεται με το gmail του ιατρείου).
2. **API Keys → Create API Key** → η τιμή μπαίνει:
   - στο `.env.local` (τοπικά) ως `RESEND_API_KEY`
   - στο Vercel → Environment Variables → `RESEND_API_KEY` (Edit → Save → Redeploy)
3. **Domains → Add domain → `preventtherapy.gr`** → προσθήκη των DNS records που
   δείχνει το Resend (SPF/DKIM) στον πάροχο του domain → αναμονή verify.
   - Μέχρι το verify, ο αποστολέας `onboarding@resend.dev` παραδίδει emails
     **μόνο στη διεύθυνση του κατόχου του Resend λογαριασμού** (αρκεί για δοκιμή).
4. Μετά το verify: αλλαγή του `EMAIL_FROM` (Vercel + `.env.local`) σε π.χ.
   `PREVENT Therapy Space <no-reply@preventtherapy.gr>`.

Παραλήπτης ειδοποιήσεων θεραπευτή: **geokap94@hotmail.com** (hardcoded στο
`lib/email.js` → `CONTACT_EMAIL`).

## 2. Συνεργάτες χωρίς στοιχεία (αφαιρέθηκαν από το site 26/8/2026)

Οι κάρτες «Σύντομα κοντά μας» αφαιρέθηκαν. Για επαναφορά, μόλις υπάρξουν στοιχεία:

### Γυμνάστρια
- Χρειάζεται: όνομα, φωτογραφία, (προαιρετικά credentials/bio)
- Επαναφορά: προσθήκη entry `{ id: "trainer", image: "/images/team/….jpg", icon: Dumbbell }`
  στο `data/partners.js` + όνομα στα `public/locales/el|en/home.json`
  (`partners.members.trainer.name`). Τα υπόλοιπα λεκτικά (role, roleDesc) υπάρχουν ήδη.

### Γενική / Οικογενειακή Ιατρός
- Χρειάζεται: όνομα, φωτογραφία, (προαιρετικά credentials/bio)
- Επαναφορά: όπως παραπάνω με `id: "doctor"`, icon `Stethoscope`.

## 3. Αθλητές (χορηγίες) — section εκτός site (26/8/2026)

Το `AthletesSection` αφαιρέθηκε από την αρχική (`app/page.jsx`) γιατί τα ονόματα
ήταν placeholders («Όνομα Αθλητή»). Ο κώδικας υπάρχει ανέπαφος.

- Χρειάζεται: πραγματικά ονόματα + αθλήματα αθλητών που χορηγεί ο χώρος
- Επαναφορά: συμπλήρωση του `athletes.items` στα
  `public/locales/el|en/home.json` + επαναφορά των δύο γραμμών
  (`import { AthletesSection } …` και `<AthletesSection />`) στο `app/page.jsx`.

## 4. Σελίδες Πολιτικής Απορρήτου & Όρων Χρήσης (26/8/2026)

Τα links «Πολιτική Απορρήτου» / «Όροι Χρήσης» αφαιρέθηκαν από το footer γιατί
οι σελίδες `/privacy` και `/terms` δεν υπάρχουν (οδηγούσαν σε 404).

- Χρειάζεται: κείμενα πολιτικής απορρήτου (σημαντικό — το booking συλλέγει
  προσωπικά δεδομένα: όνομα, email, τηλέφωνο / GDPR) και όρων χρήσης.
- Επαναφορά: δημιουργία `app/privacy/page.jsx` και `app/terms/page.jsx` +
  επαναφορά των δύο `<Link>` στο `components/physio/PhysioFooter.jsx`
  (τα λεκτικά `footer:legal.privacy` / `footer:legal.terms` υπάρχουν ήδη).

## 5. Λοιπά περιεχομένου

- **Τηλέφωνο site**: παντού το placeholder `210 123 4567` / `tel:+302101234567`
  (header, footer). Χρειάζεται το πραγματικό τηλέφωνο του ιατρείου.
- **Φωτογραφική γλώσσα** (από design critique): η φωτό Τσιτουρίδη (κοστούμι,
  εξωτερική) δεν ταιριάζει με το ζεστό ύφος — ιδανικά νέα φωτογράφιση όλης της
  ομάδας μέσα στον χώρο.
- **Αποστολέας Google Calendar invites**: εμφανίζεται ο προσωπικός λογαριασμός
  (giorgoskapatsinas@gmail.com). Αν προτιμηθεί λογαριασμός «ιατρείου», φτιάχνεται
  Google λογαριασμός, μοιράζεται/μεταφέρεται το ημερολόγιο και ξανατρέχει το
  `npm run google:setup` με αυτόν (νέο refresh token σε `.env.local` + Vercel).
